apiVersion: v2
name: nfs-provisioner
description: NFS subdir external provisioner for NAS-backed StorageClass
version: 1.0.0
type: application
dependencies:
  - name: nfs-subdir-external-provisioner
    version: 4.0.18
    repository: https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner/
